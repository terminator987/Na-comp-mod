# True when the SmartTaxes mod is active.
glorpui_is_smart_taxes_active = {
	scope = country

	is_shown = {
		is_target_in_global_variable_list = {
			name = cmf_active_mod_ids
			target = flag:smart_taxes
		}
	}
	effect = { }
}
