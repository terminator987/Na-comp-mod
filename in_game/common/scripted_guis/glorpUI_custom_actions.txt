SetUseVanillaPortrait = {
    effect = {
        if = {
            limit = { NOT = { has_variable = useVanillaPortrait } }
            set_variable = { name = useVanillaPortrait value = 1 }
        }
        else = {
            remove_variable = useVanillaPortrait
        }
    }
}

SetUseStaticFlag = {
    effect = {
        if = {
            limit = { NOT = { has_variable = useStaticFlag } }
            set_variable = { name = useStaticFlag value = 1 }
        }
        else = {
            remove_variable = useStaticFlag
        }
    }
}

# This is backwards at the moment, setting this variable actually disables the Frame on the Static Flag.
# I wanted the Framed Static Flag to be the default when you toggle the Static Flag on
SetUseFramedStaticFlag = {
    effect = {
        if = {
            limit = { NOT = { has_variable = useFramedStaticFlag } }
            set_variable = { name = useFramedStaticFlag value = 1 }
        }
        else = {
            remove_variable = useFramedStaticFlag
        }
    }
}

SetUseDefaultIconsBGLeftPanel = {
    effect = {
        if = {
            limit = { NOT = { has_variable = useDefaultIconsBGLeftPanel } }
            set_variable = { name = useDefaultIconsBGLeftPanel value = 1 }
        }
        else = {
            remove_variable = useDefaultIconsBGLeftPanel
        }
    }
}

SetUseDefaultIconsLeftPanel = {
    effect = {
        if = {
            limit = { NOT = { has_variable = useDefaultIconsLeftPanel } }
            set_variable = { name = useDefaultIconsLeftPanel value = 1 }
        }
        else = {
            remove_variable = useDefaultIconsLeftPanel
        }
    }
}

SetUseDefaultIconsRightPanel = {
    effect = {
        if = {
            limit = { NOT = { has_variable = useDefaultIconsRightPanel } }
            set_variable = { name = useDefaultIconsRightPanel value = 1 }
        }
        else = {
            remove_variable = useDefaultIconsRightPanel
        }
    }
}

### TOP BAR ###
SetDisableTopBarIncome = {
    effect = {
        if = {
            limit = { NOT = { has_variable = disableTopBarIncome } }
            set_variable = { name = disableTopBarIncome value = 1 }
        }
        else = {
            remove_variable = disableTopBarIncome
        }
    }
}

SetDisableTopBarStability = {
    effect = {
        if = {
            limit = { NOT = { has_variable = disableTopBarStability } }
            set_variable = { name = disableTopBarStability value = 1 }
        }
        else = {
            remove_variable = disableTopBarStability
        }
    }
}

SetDisableTopBarPopulation = {
    effect = {
        if = {
            limit = { NOT = { has_variable = disableTopBarPopulation } }
            set_variable = { name = disableTopBarPopulation value = 1 }
        }
        else = {
            remove_variable = disableTopBarPopulation
        }
    }
}

SetDisableTopBarLegitimacy = {
    effect = {
        if = {
            limit = { NOT = { has_variable = disableTopBarLegitimacy } }
            set_variable = { name = disableTopBarLegitimacy value = 1 }
        }
        else = {
            remove_variable = disableTopBarLegitimacy
        }
    }
}

SetDisableTopBarPrestige = {
    effect = {
        if = {
            limit = { NOT = { has_variable = disableTopBarPrestige } }
            set_variable = { name = disableTopBarPrestige value = 1 }
        }
        else = {
            remove_variable = disableTopBarPrestige
        }
    }
}

SetDisableTopBarDiplomats = {
    effect = {
        if = {
            limit = { NOT = { has_variable = disableTopBarDiplomats } }
            set_variable = { name = disableTopBarDiplomats value = 1 }
        }
        else = {
            remove_variable = disableTopBarDiplomats
        }
    }
}

SetEnableDynamicWarStats = {
    effect = {
        if = {
            limit = { NOT = { has_variable = dynamicWarStats } }
            set_variable = { name = dynamicWarStats value = 1 }
        }
        else = {
            remove_variable = dynamicWarStats
        }
    }
}

SetDisableLeftPanelStatsBar = {
    effect = {
        if = {
            limit = { NOT = { has_variable = disableLeftPanelStatsBar } }
            set_variable = { name = disableLeftPanelStatsBar value = 1 }
        }
        else = {
            remove_variable = disableLeftPanelStatsBar
        }
    }
}

SetDisableColouredMapmodeButtons = {
    effect = {
        if = {
            limit = { NOT = { has_variable = disableColouredMapmodeButtons } }
            set_variable = { name = disableColouredMapmodeButtons value = 1 }
			cmf_change_variable_map = {
				name = cmm
				key = flag:glorpui__disableColouredMapmodeButtons
				value = 0
			}
        }
        else = {
            remove_variable = disableColouredMapmodeButtons
			cmf_change_variable_map = {
				name = cmm
				key = flag:glorpui__disableColouredMapmodeButtons
				value = 1
			}
        }
    }
}

SetToggleLeftPanelStatsBar = {
    effect = {
        if = {
            limit = { NOT = { has_variable = toggleLeftPanelStatsBar } }
            set_variable = { name = toggleLeftPanelStatsBar value = 1 }
        }
        else = {
            remove_variable = toggleLeftPanelStatsBar
        }
    }
}

EnableMassRecruiting = {
	effect = {
		if = { # Set
			limit = {
				NOT = { has_variable = massRecruiting }
			}
			set_variable = { name = massRecruiting value = 5 }
		}
		else = { # Unset
			remove_variable = massRecruiting
		}
	}
}

ClearMassRecruiting = {
	effect = {
		remove_variable = massRecruiting
	}
}

UpdateMassRecruiting = {
	effect = {
		if = { # Iterate
			limit = {
				var:massRecruiting > 1
			}
			change_variable = {
				name = massRecruiting
				subtract = 1
			}
		}
		else = { # Unset
			remove_variable = massRecruiting
		}
	}
}

SetVanillaMapmodeWidth = {
	effect = {
		if = {
			limit = { NOT = { has_variable = vanillaMapmodeWidth } }
			set_variable = { name = vanillaMapmodeWidth value = 1 }
			cmf_change_variable_map = {
				name = cmm
				key = flag:glorpui__vanillaMapmodeWidth
				value = 1
			}
		}
		else = {
			remove_variable = vanillaMapmodeWidth
			cmf_change_variable_map = {
				name = cmm
				key = flag:glorpui__vanillaMapmodeWidth
				value = 0
			}
		}
	}
}

SetVanillaMapmodeHeight = {
	effect = {
		if = {
			limit = { NOT = { has_variable = vanillaMapmodeHeight } }
			set_variable = { name = vanillaMapmodeHeight value = 1 }
			cmf_change_variable_map = {
				name = cmm
				key = flag:glorpui__vanillaMapmodeHeight
				value = 1
			}
		}
		else = {
			remove_variable = vanillaMapmodeHeight
			cmf_change_variable_map = {
				name = cmm
				key = flag:glorpui__vanillaMapmodeHeight
				value = 0
			}
		}
	}
}

RefreshAvgStats = {
	effect = {
		glorpui_sync_avg_stats = yes
	}
}

SetControlStatToggle = {
	effect = {
		if = {
			limit = { NOT = { has_variable = controlStatToggle } }
			set_variable = { name = controlStatToggle value = 1 }
		}
		else = {
			remove_variable = controlStatToggle
		}
	}
}

### BUILD PANEL ###
SetRoiUseProfit = {
    effect = {
        if = {
            limit = { NOT = { has_variable = roiUseProfit } }
            set_variable = { name = roiUseProfit value = 1 }
        }
        else = {
            remove_variable = roiUseProfit
        }
    }
}

SetRoiUseMonths = {
    effect = {
        if = {
            limit = { NOT = { has_variable = roiUseMonths } }
            set_variable = { name = roiUseMonths value = 1 }
        }
        else = {
            remove_variable = roiUseMonths
        }
    }
}
